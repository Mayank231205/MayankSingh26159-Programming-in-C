/*Experiment 2: Opertors
    2. WAP a C program to convert temprature from celcius to fahrenheit using the formula: f = (C * 9/5) + 32> */
    #include<stdio.h>
     
     int main()
     {
     	double c,f;
     	printf("Mayank Singh\nSAP ID : 590026159\nCourse- Bsc Cs\nBatch-1");
    	printf("\n---------------------------------------------------------------------\n");
    	printf("Enter the temptature in celcius");
    	scanf("%lf",&c);
    	f= (c *9/5) +32;
    	printf("The entered temprature in fahrenhite is: %lf", f);
    	return 0;
	 }
    
